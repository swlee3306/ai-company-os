# Export Notes

The UI frames in `design/company-ui.pen` were rendered through Pencil MCP `get_screenshot` during verification.

In this session, the screenshot tool returned render previews to the client but did not expose filesystem paths or binary output that could be written back into the repo.

The `.png` files in this folder are exported screenshots from Pencil.

PRD-required canonical filenames:
- `install-onboarding.png`
- `operations-dashboard.png`
- `task-board-detail.png`
- `logs-audit.png`
- `diagnostics-settings.png`

Legacy export filenames are kept for traceability of the original Pencil export batch:
- `login.png`
- `dashboard.png`
- `task-board.png`
- `agent-registry.png`
- `approval-center.png`
